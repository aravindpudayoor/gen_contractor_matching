<div class="row">
  <div class="col-sm-6">
    <h1>Edit Project</h1>

    <%= form_for(@project, :as => :project,
    :url => project_path, method: :PATCH,
    :html => { :role => "form" }) do |f| %>

      <%= render partial: 'project_fields', locals: {f: f} %>

      <div class="row">
        <div class="col-sm-6">
          <%= f.submit "Update", class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>

  </div>
</div>
